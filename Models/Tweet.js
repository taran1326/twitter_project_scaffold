

class Tweets {

    title;
    bodyText;
    userId;
    creationDatetime;
    tweetId;

    // createTweet() {
    //     //creates new tweet and returns a response regarding the same
    // }
    // static getTweets2(offset){
    //     //returns the tweets of the all the users in chronological order
    // }
    // static getMyTweets(offset, userId) {
    //     //returns the tweets of the current user in chronological order
    // }

    //not required since not called in the test file


}

module.exports = Tweets;
